import { PackageJson } from 'type-fest';
interface PackageJsonSet {
    name: string;
    dependencies: Set<string>;
}
interface Params {
    packageJsonContents: PackageJson[];
}
export declare function getPackagesOrder({ packageJsonContents, }: Params): PackageJsonSet[];
export {};
