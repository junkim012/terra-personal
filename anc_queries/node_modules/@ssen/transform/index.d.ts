export interface Transformer {
    /**
     * Transform the file to an source text
     *
     * @param file An absolute path like /absolute-path/some.svg
     */
    getSourceText: (option?: Record<string, string>) => (file: string) => string;
}
export declare const plainTextTransformer: Transformer;
export declare const imageTransformer: Transformer;
export declare const yamlTransformer: Transformer;
export declare const svgTransformer: Transformer;
