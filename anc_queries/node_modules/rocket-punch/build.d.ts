import { BuildParams } from './params';
export declare function build({ cwd, sourceRoot, strict, showPackagesOrder, dist, tsconfig, entry, svg, transformPackageJson, transformCompilerHost, transformCompilerOptions, emitCustomTransformers, onMessage, }: BuildParams): Promise<void>;
