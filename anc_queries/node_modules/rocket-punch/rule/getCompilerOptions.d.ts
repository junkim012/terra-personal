import ts from 'typescript';
import { PackageInfo } from '../types';
interface Params {
    searchPath: string;
    configName: string;
    packageInfo: PackageInfo;
    buildType: 'module' | 'commonjs';
    declaration: boolean;
}
export declare function getCompilerOptions({ searchPath, configName, packageInfo, buildType, declaration, }: Params): ts.CompilerOptions;
export {};
